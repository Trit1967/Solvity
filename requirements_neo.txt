# GPT-Neo RAG Requirements
# All open-source, no API keys needed

# Core ML frameworks
torch>=2.0.0  # For CPU, use torch==2.0.0+cpu
transformers>=4.35.0
accelerate>=0.24.0

# LangChain for RAG
langchain>=0.1.0
langchain-community>=0.0.10

# Vector database (local)
faiss-cpu>=1.7.4  # Use faiss-gpu for CUDA

# Embeddings
sentence-transformers>=2.2.2

# Document processing
pypdf>=3.17.0
unstructured>=0.11.0
python-magic>=0.4.27  # For file type detection
python-magic-bin>=0.4.14  # Windows only

# Optional optimizations
bitsandbytes>=0.41.0  # For 8-bit quantization (Linux/WSL only)

# Utilities
tqdm>=4.65.0
numpy>=1.24.0